{
  "name": "esbuild-resolution-mounted-volume",
  "license": "Unlicense",
  "packageManager": "yarn@4.13.0",
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=dist/out.js"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
